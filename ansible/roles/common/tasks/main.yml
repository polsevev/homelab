---
- name: Install common dependencies
  ansible.builtin.apt:
    pkg:
      - acl
      - prometheus-node-exporter
      - btop
      - ca-certificates
      - curl
      - python3
      - python3-pip
    update_cache: true
  become: true
